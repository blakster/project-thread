{
  "name": "hearth",
  "version": "0.1.0",
  "private": true,
  "description": "Named teammates in a project thread, with a local connected computer and isolated per-agent files.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js start",
    "computer": "node dist/index.js computer",
    "test": "tsc && node --test test/slice1.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "typescript": "^7.0.2"
  }
}
